import 'package:flutter/material.dart';
class ConnterView extends StatelessWidget {
  const ConnterView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Contador Con MVVM')
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: <Widget>[
            

          ]    
        ),
      )
    );
  }
}